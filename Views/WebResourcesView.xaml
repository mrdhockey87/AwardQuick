<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:viewModels="clr-namespace:AwardQuick.ViewModels"
             xmlns:overlay="clr-namespace:AwardQuick.Overlays"
             x:Class="AwardQuick.Views.WebResourcesView"
             Title="Web Resources">
    <ContentPage.BindingContext>
        <viewModels:WebResourcesViewModel x:Name="WebResourcesViewModel" />
    </ContentPage.BindingContext>

    <ContentPage.Content>
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="*" />
            </Grid.RowDefinitions>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>
            <overlay:ProgressBarOverlay x:Name="progressBarOverlay"
                                        Grid.Row="1"
                                        Grid.Column="0" />
        </Grid>
    </ContentPage.Content>
</ContentPage>